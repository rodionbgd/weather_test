1. Организация структуры проекта. На какие модули разбить.
2. Рендер страницы после загрузки всех скриптов, т.е. юзер видит вначале анимацию, затем полностью отрендеренную страницу.
3. В функции createMap вызываются функции:  setCurrentCityWeather(optionsHour), addCityToList(),  flyToCity(coords), createMarker(coords, optionsHour.name);
Хотелось бы вынести вызов addCityToList().
4. В функции getCityByCoords вызывает createMap, нужно, чтобы она возвращала название города.
5. Тестирование:
    а) при тестировании вызова функции init при document.dispatchEvent(new Event("DOMContentLoaded", {bubbles: true, cancelable: true}));
пишет, что не определен метод tileLayer в window.WE. Пробовал так: window.WE = jest.fn(), но не помогло.
    б) в приложении экспортируются несколько функций всего, как тестировать неэкспортируемые функции?
    в) тестирование внешних API?
6. Нормальная прокрутка на тачскрине, потому что скроллится не только слайдер но и страница.
7. Подключение внешних скриптов, в js или html?